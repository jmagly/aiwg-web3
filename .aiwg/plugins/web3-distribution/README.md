# Web3 distribution wrapper

This wrapper packages the framework in payload/. Canonical capability files live in payload/src/. Use the repository README for install and development instructions. This is a development scaffold, not a production release.
